#include<stdio.h>
void main()
{
int i,n=37,found=0;
for(i=2;i<=n/2;i++)
{
if(n % i == 0)
    {
  printf("smallest factor is=%d\n",i);
  found=1;
    break;
}
}
if(found==0)
printf("Smallest factor = %d",n);

}
