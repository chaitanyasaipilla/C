#include<stdio.h>
void main()
{
int i,n,count=0;
    printf("enter num:");
    scanf("%d",&n);
for(i=2;i<=n/2;i++)
{
if(n % i == 0)
        count++;
}
if(count==0)
    printf("PRIME NUMBER");
else
    printf("NOT A PRIME NUMBER");
}
