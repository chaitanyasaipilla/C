#include<stdio.h>
void main()
{
    char s[20];
    int i,digit;

    printf("enter a string:");
    gets(s);

    for(i=0;s[i]!='\0';i++)
    {
        if(isdigit(s[i]))
            putch(s[i]);
    }

}
