#include<stdio.h>
void main()
{
    char ch[10];
   int i, count=0;
    printf("enter the string:");
    gets(ch);

    for(i=0;ch[i]!='\0';i++)
    {
        if(isupper(ch[i]))
            count++;
    }
          printf("Count = %d", count);
    }
